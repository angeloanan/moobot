export enum MEASUREMENT_NAMES {
  EXP_GAIN = 'exp_gain',
  INVITE = 'invite'
}

export enum EXP_GAIN_EVENTS {
  CHAT = 'chat',
  VOICE_CHAT = 'voice_chat'
}

// ---
export enum METRICS_MEASUREMENT_NAMES {
  MEMORY = 'memory',
  LATENCY = 'latency',
  PROCESS_GC = 'process_gc',
  PROCESS_FILE_DESCRIPTORS = 'process_fds',
  PROCESS_EVENT_LOOP_LAG = 'event_loop_lag'
}
